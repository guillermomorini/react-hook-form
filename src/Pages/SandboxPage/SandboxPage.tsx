import { SubmitHandler, useForm } from "react-hook-form";

type Input = {
  name: string;
};

export const SandboxPage: React.FC = () => {
  return (
    <div>
      <h3>Sandbox page</h3>
    </div>
  );
};
